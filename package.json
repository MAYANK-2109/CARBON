{
  "name": "carbon-footprint-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Professional-grade Carbon Footprint Assistant — Track, analyze, and reduce your climate impact.",
  "workspaces": [
    "shared",
    "server",
    "client",
    "e2e"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "test": "npm run test -w shared && npm run test -w server && npm run test -w client",
    "test:coverage": "npm run test:coverage -w server && npm run test:coverage -w client",
    "test:e2e": "npm run test -w e2e",
    "lint": "npm run lint -w client && npm run lint -w server",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "audit": "npm audit --audit-level=moderate",
    "analyze": "npm run build -w client -- --mode analyze",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix --max-warnings=0",
      "prettier --write"
    ],
    "*.{json,md,yml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@ampproject/remapping": "^2.3.0",
    "concurrently": "^9.0.1",
    "husky": "9.1.7",
    "lint-staged": "16.4.0",
    "prettier": "3.8.4",
    "typescript": "~6.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
